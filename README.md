# cli

Internal fork of [urfave/cli](https://github.com/urfave/cli) v1 for `bm-cli` command parsing.

Module: `github.com/buckit-io/cli`

Not intended for external use — kept frozen at urfave v1.20.0 semantics.
